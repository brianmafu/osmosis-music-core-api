version: '3'

services:
  web:
      build:
          context: .
      command: 
        - python /app/manage.py runserver 0.0.0.0:8000"
      container_name: "osmosisapi"
      ports:
          - 80:8000
      volumes: 
          - .:/app

